name = "my-portfolio"
compatibility_date = "2025-04-27"

[build]
  command = "npm run build"  # Ensure the build command runs before deploying

[site]
  bucket = "./build"  # Point to the build folder generated by React
  entry-point = "workers-site"  # This tells Cloudflare to treat the build folder as the static assets directory
